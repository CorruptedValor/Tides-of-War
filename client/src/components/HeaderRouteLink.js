// import React from 'react';

// export default ({ to, ...rest }) => (
//   <Route path={to} children={({ match }) => (
//     <li className={match ? 'active' : ''}>
//         <Link to={to} {...rest}/>
//     </li>
//   )}/>
// );